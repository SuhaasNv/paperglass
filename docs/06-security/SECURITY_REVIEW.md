# Security review

To be written at US-080 (week 11). Shape: every row of `THREAT_MODEL.md` with the test or command that proves the control, a before-and-after for anything changed by the review, four abuse scenarios run and recorded (zip bomb, 10,000-page PDF, font with an enormous glyph count, polyglot PDF and DOCX), the external reviewer's findings if an advisor accepted, and a list of what production deployments still need.
