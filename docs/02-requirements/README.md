# 02-requirements

Requirements with ids and the five use cases with acceptance criteria. Written 22 Sep 2026, before code. Use cases are numbered like the releases (UC1 is v0.1.0).

| Document | What it holds |
|----------|---------------|
| `REQUIREMENTS.md` | FR (functional), NFR (non-functional), SEC (security), BENCH (benchmark), REP (report) requirements, each with an id, a release and a test or command that proves it |
| `USE_CASES.md` | UC1 to UC5 with background, the story, acceptance criteria, and the stories that implement them |

Start with `USE_CASES.md`. Related: `../../SCOPE.md`, `../05-planning/ISSUES.md`.
