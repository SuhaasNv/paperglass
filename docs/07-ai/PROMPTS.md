# Prompts used by the product

None. Paperglass v0.1.0 to v0.5.0 use no language model at runtime. If the deep tier is built, its prompt (if any) is recorded here with a version and a hash, and the version is stamped into every report that used it.
