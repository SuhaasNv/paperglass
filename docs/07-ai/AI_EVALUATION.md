# AI evaluation

To be written if the deep tier (COULD, US-090) is built. Shape: the evaluation set (scans and low-contrast images with ground truth, none real), the metric (recall on the set, false positives on the benign scans), the model version and weights hash, the command, and the date. Until then the product uses no model.
