# 07-ai

The optional AI inside the product: an instruction-likeness hook that raises severity on hidden regions, and the opt-in deep tier. This is separate from the AI used to build Paperglass (`../../AI_USAGE.md`). Written 22 Sep 2026.

| Document | What it holds |
|----------|---------------|
| `CLASSIFIER_DESIGN.md` | The hook: what it may and may not do, the phrase packs, the plug-in interface |
| `AI_EVALUATION.md` | Evaluation set and results for the deep tier (to be written if the deep tier is built) |
| `PROMPTS.md` | Any prompt used by the product, versioned (none today) |

Related: `../03-architecture/TECHNIQUE_REGISTRY.md` (`text.instruction.hint` is a modifier).
