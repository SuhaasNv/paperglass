# LlamaIndex (v0.4.0, US-061)

Planned. `PaperglassPostprocessor` (a `BaseNodePostprocessor`) applies the shared contract to nodes; `PaperglassReader` wraps a reader so files are scanned as they are loaded. Extra: `paperglass[llamaindex]` (pins `llama-index-core`). Tested against the pinned version. Same limit as LangChain for text-only nodes.
